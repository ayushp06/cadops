# CadOps

CadOps is a CAD-aware command-line workflow layer over Git and Git LFS. The MVP provides safe repository initialization, CAD-aware status output, and repository health checks for file-based CAD workflows.

## Commands

- `cadops init`
- `cadops status`
- `cadops doctor`

## Development

```bash
make test
```
