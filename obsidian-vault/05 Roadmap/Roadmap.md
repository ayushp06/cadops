# Roadmap

Near-term work includes richer status UX, safer repo policy validation, deeper CAD-aware history views beyond the initial recent-commit output, and snapshot/watch capabilities such as preview generation, optional inclusion of non-CAD files, and smarter commit grouping after the core flows stabilize. Lock command polish can follow with richer lock owner/status inspection once the basic Git LFS lock and unlock flows are in place.
