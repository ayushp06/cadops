# Repository Structure

The repository is organized by CLI entrypoints, internal packages, templates, docs, and an Obsidian vault for product and engineering notes. Repository-owned generated metadata lives under `.cadops/metadata/` as a single JSON manifest. V1 preview records live under `.cadops/previews/` as a separate JSON manifest.
