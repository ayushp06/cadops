# MVP Scope

CadOps MVP intentionally covers only three commands:

- `cadops init`
- `cadops status`
- `cadops doctor`

The product goal is safe Git and Git LFS setup for CAD-heavy repositories without introducing a new version control model.
